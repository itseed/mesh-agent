# MeshAgent — Plan 1: Foundation

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** ตั้ง monorepo structure, shared types, PostgreSQL schema, Docker Compose dev environment, Fastify API server พร้อม JWT auth

**Architecture:** pnpm workspaces monorepo มี `apps/api` (Fastify) และ `packages/shared` (Drizzle schema + types) Docker Compose รัน PostgreSQL 16 + Redis 7 ใน dev การ authenticate ใช้ JWT single-user (email + password เก็บใน env)

**Tech Stack:** Node.js 20, pnpm 9, TypeScript 5, Fastify 4, Drizzle ORM 0.30, PostgreSQL 16, Redis 7, Vitest 1, Docker Compose 2

---

## File Map

```
meshagent/
├── package.json                        ← pnpm workspace root
├── pnpm-workspace.yaml
├── tsconfig.base.json                  ← shared TS config
├── .env.example
├── docker-compose.yml                  ← dev: postgres + redis
├── packages/
│   └── shared/
│       ├── package.json
│       ├── tsconfig.json
│       └── src/
│           ├── index.ts                ← re-exports
│           ├── types.ts                ← Task, Agent, Project, User types
│           └── schema.ts               ← Drizzle table definitions
└── apps/
    └── api/
        ├── package.json
        ├── tsconfig.json
        └── src/
            ├── index.ts                ← server entry + start
            ├── server.ts               ← buildServer() factory
            ├── env.ts                  ← env validation (zod)
            ├── plugins/
            │   ├── db.ts               ← Drizzle + postgres plugin
            │   └── redis.ts            ← Redis plugin
            ├── routes/
            │   └── auth.ts             ← POST /auth/login, GET /auth/me
            └── __tests__/
                ├── auth.test.ts
                └── health.test.ts
```

---

## Task 1: Workspace Root Setup

**Files:**

- Create: `package.json`
- Create: `pnpm-workspace.yaml`
- Create: `tsconfig.base.json`
- Create: `.env.example`
- Create: `.gitignore`

- [ ] **Step 1: Create workspace root `package.json`**

```json
{
  "name": "meshagent",
  "private": true,
  "scripts": {
    "dev:api": "pnpm --filter api dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  }
}
```

- [ ] **Step 2: Create `pnpm-workspace.yaml`**

```yaml
packages:
  - 'apps/*'
  - 'packages/*'
```

- [ ] **Step 3: Create `tsconfig.base.json`**

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "strict": true,
    "skipLibCheck": true,
    "esModuleInterop": true,
    "resolveJsonModule": true
  }
}
```

- [ ] **Step 4: Create `.env.example`**

```env
# PostgreSQL
DATABASE_URL=postgresql://meshagent:meshagent@localhost:5432/meshagent

# Redis
REDIS_URL=redis://localhost:6379

# Auth — single user
AUTH_EMAIL=admin@example.com
AUTH_PASSWORD=changeme
JWT_SECRET=change-this-to-a-random-64-char-string

# Server
PORT=3001
NODE_ENV=development
```

- [ ] **Step 5: Create `.gitignore`**

```
node_modules/
dist/
.env
*.local
```

- [ ] **Step 6: Initialize pnpm and create workspace directories**

```bash
pnpm init -y  # ถ้ายังไม่มี pnpm ให้รัน: npm install -g pnpm@9
mkdir -p apps/api packages/shared
```

- [ ] **Step 7: Commit**

```bash
git add package.json pnpm-workspace.yaml tsconfig.base.json .env.example .gitignore
git commit -m "chore: initialize pnpm monorepo workspace"
```

---

## Task 2: Shared Package — Types

**Files:**

- Create: `packages/shared/package.json`
- Create: `packages/shared/tsconfig.json`
- Create: `packages/shared/src/types.ts`
- Create: `packages/shared/src/index.ts`

- [ ] **Step 1: Create `packages/shared/package.json`**

```json
{
  "name": "@meshagent/shared",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
```

- [ ] **Step 2: Create `packages/shared/tsconfig.json`**

```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src"
  },
  "include": ["src"]
}
```

- [ ] **Step 3: Create `packages/shared/src/types.ts`**

```typescript
export type KanbanStage = 'backlog' | 'in_progress' | 'review' | 'done';

export type AgentRole =
  | 'frontend'
  | 'backend'
  | 'mobile'
  | 'devops'
  | 'designer'
  | 'qa'
  | 'reviewer';

export type AgentStatus = 'idle' | 'running' | 'error';

export interface Task {
  id: string;
  title: string;
  description: string | null;
  stage: KanbanStage;
  agentRole: AgentRole | null;
  projectId: string | null;
  githubPrUrl: string | null;
  createdAt: Date;
  updatedAt: Date;
}

export interface Agent {
  id: string;
  role: AgentRole;
  status: AgentStatus;
  currentTaskId: string | null;
  projectId: string | null;
}

export interface Project {
  id: string;
  name: string;
  paths: Record<string, string>;
  githubRepos: string[];
  isActive: boolean;
  createdAt: Date;
}
```

- [ ] **Step 4: Create `packages/shared/src/index.ts`**

```typescript
export * from './types.js';
```

- [ ] **Step 5: Install shared deps**

```bash
cd packages/shared && pnpm install
```

- [ ] **Step 6: Commit**

```bash
git add packages/shared/
git commit -m "feat(shared): add domain types"
```

---

## Task 3: Shared Package — Database Schema

**Files:**

- Modify: `packages/shared/package.json` (add drizzle deps)
- Create: `packages/shared/src/schema.ts`
- Modify: `packages/shared/src/index.ts`

- [ ] **Step 1: Add Drizzle dependencies**

```bash
cd packages/shared
pnpm add drizzle-orm postgres
pnpm add -D drizzle-kit
```

- [ ] **Step 2: Create `packages/shared/src/schema.ts`**

```typescript
import { pgTable, text, timestamp, boolean, jsonb } from 'drizzle-orm/pg-core';

export const tasks = pgTable('tasks', {
  id: text('id')
    .primaryKey()
    .$defaultFn(() => crypto.randomUUID()),
  title: text('title').notNull(),
  description: text('description'),
  stage: text('stage', {
    enum: ['backlog', 'in_progress', 'review', 'done'],
  })
    .notNull()
    .default('backlog'),
  agentRole: text('agent_role', {
    enum: ['frontend', 'backend', 'mobile', 'devops', 'designer', 'qa', 'reviewer'],
  }),
  projectId: text('project_id').references(() => projects.id),
  githubPrUrl: text('github_pr_url'),
  createdAt: timestamp('created_at').notNull().defaultNow(),
  updatedAt: timestamp('updated_at').notNull().defaultNow(),
});

export const projects = pgTable('projects', {
  id: text('id')
    .primaryKey()
    .$defaultFn(() => crypto.randomUUID()),
  name: text('name').notNull().unique(),
  paths: jsonb('paths').notNull().$type<Record<string, string>>().default({}),
  githubRepos: jsonb('github_repos').notNull().$type<string[]>().default([]),
  isActive: boolean('is_active').notNull().default(false),
  createdAt: timestamp('created_at').notNull().defaultNow(),
});
```

- [ ] **Step 3: Update `packages/shared/src/index.ts`**

```typescript
export * from './types.js';
export * from './schema.js';
```

- [ ] **Step 4: Commit**

```bash
git add packages/shared/
git commit -m "feat(shared): add Drizzle schema for tasks and projects"
```

---

## Task 4: Docker Compose Dev Environment

**Files:**

- Create: `docker-compose.yml`

- [ ] **Step 1: Create `docker-compose.yml`**

```yaml
services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: meshagent
      POSTGRES_PASSWORD: meshagent
      POSTGRES_DB: meshagent
    ports:
      - '5432:5432'
    volumes:
      - db_data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U meshagent']
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    ports:
      - '6379:6379'
    volumes:
      - redis_data:/data
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  db_data:
  redis_data:
```

- [ ] **Step 2: Start services and verify**

```bash
docker compose up -d
docker compose ps
```

Expected: db and redis สถานะ `healthy`

- [ ] **Step 3: Commit**

```bash
git add docker-compose.yml
git commit -m "chore: add Docker Compose dev environment (postgres + redis)"
```

---

## Task 5: API Server Setup

**Files:**

- Create: `apps/api/package.json`
- Create: `apps/api/tsconfig.json`
- Create: `apps/api/src/env.ts`
- Create: `apps/api/src/server.ts`
- Create: `apps/api/src/index.ts`
- Create: `apps/api/src/plugins/db.ts`
- Create: `apps/api/src/plugins/redis.ts`

- [ ] **Step 1: Create `apps/api/package.json`**

```json
{
  "name": "api",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@fastify/jwt": "^8.0.0",
    "@meshagent/shared": "workspace:*",
    "drizzle-orm": "^0.30.0",
    "fastify": "^4.27.0",
    "ioredis": "^5.3.2",
    "postgres": "^3.4.4",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.7.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
```

- [ ] **Step 2: Create `apps/api/tsconfig.json`**

```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src"
  },
  "include": ["src"]
}
```

- [ ] **Step 3: Create `apps/api/src/env.ts`**

```typescript
import { z } from 'zod';

const envSchema = z.object({
  DATABASE_URL: z.string().url(),
  REDIS_URL: z.string().url(),
  AUTH_EMAIL: z.string().email(),
  AUTH_PASSWORD: z.string().min(8),
  JWT_SECRET: z.string().min(32),
  PORT: z.coerce.number().default(3001),
  NODE_ENV: z.enum(['development', 'production', 'test']).default('development'),
});

export const env = envSchema.parse(process.env);
```

- [ ] **Step 4: Create `apps/api/src/plugins/db.ts`**

```typescript
import fp from 'fastify-plugin';
import { drizzle } from 'drizzle-orm/postgres-js';
import postgres from 'postgres';
import { env } from '../env.js';
import * as schema from '@meshagent/shared';

declare module 'fastify' {
  interface FastifyInstance {
    db: ReturnType<typeof drizzle>;
  }
}

export default fp(async (fastify) => {
  const client = postgres(env.DATABASE_URL);
  const db = drizzle(client, { schema });
  fastify.decorate('db', db);
  fastify.addHook('onClose', async () => {
    await client.end();
  });
});
```

- [ ] **Step 5: Add `fastify-plugin` dependency**

```bash
cd apps/api && pnpm add fastify-plugin
```

- [ ] **Step 6: Create `apps/api/src/plugins/redis.ts`**

```typescript
import fp from 'fastify-plugin';
import Redis from 'ioredis';
import { env } from '../env.js';

declare module 'fastify' {
  interface FastifyInstance {
    redis: Redis;
  }
}

export default fp(async (fastify) => {
  const redis = new Redis(env.REDIS_URL);
  fastify.decorate('redis', redis);
  fastify.addHook('onClose', async () => {
    redis.disconnect();
  });
});
```

- [ ] **Step 7: Create `apps/api/src/server.ts`**

```typescript
import Fastify from 'fastify';
import fjwt from '@fastify/jwt';
import { env } from './env.js';
import dbPlugin from './plugins/db.js';
import redisPlugin from './plugins/redis.js';

export async function buildServer() {
  const fastify = Fastify({
    logger: env.NODE_ENV !== 'test',
  });

  await fastify.register(fjwt, { secret: env.JWT_SECRET });
  await fastify.register(dbPlugin);
  await fastify.register(redisPlugin);

  fastify.get('/health', async () => ({ status: 'ok' }));

  return fastify;
}
```

- [ ] **Step 8: Create `apps/api/src/index.ts`**

```typescript
import { buildServer } from './server.js';
import { env } from './env.js';

const server = await buildServer();

try {
  await server.listen({ port: env.PORT, host: '0.0.0.0' });
} catch (err) {
  server.log.error(err);
  process.exit(1);
}
```

- [ ] **Step 9: Install API deps**

```bash
cd apps/api && pnpm install
```

- [ ] **Step 10: Commit**

```bash
git add apps/api/
git commit -m "feat(api): add Fastify server with DB and Redis plugins"
```

---

## Task 6: Database Migration

**Files:**

- Create: `packages/shared/drizzle.config.ts`
- Create: `packages/shared/src/migrate.ts`

- [ ] **Step 1: Create `packages/shared/drizzle.config.ts`**

```typescript
import { defineConfig } from 'drizzle-kit';

export default defineConfig({
  schema: './src/schema.ts',
  out: './drizzle',
  dialect: 'postgresql',
  dbCredentials: {
    url: process.env.DATABASE_URL!,
  },
});
```

- [ ] **Step 2: Add migration script to `packages/shared/package.json`**

เพิ่มใน `scripts`:

```json
{
  "scripts": {
    "typecheck": "tsc --noEmit",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:studio": "drizzle-kit studio"
  }
}
```

- [ ] **Step 3: Generate migrations**

```bash
cd packages/shared
cp ../../.env.example ../../.env  # ถ้ายังไม่มี .env
source ../../.env && DATABASE_URL=$DATABASE_URL pnpm db:generate
```

Expected: สร้างโฟลเดอร์ `drizzle/` พร้อม SQL migration files

- [ ] **Step 4: Run migration**

```bash
source ../../.env && DATABASE_URL=$DATABASE_URL pnpm db:migrate
```

Expected: ไม่มี error, ตาราง tasks และ projects ถูกสร้าง

- [ ] **Step 5: Verify tables exist**

```bash
docker compose exec db psql -U meshagent -c "\dt"
```

Expected: เห็น `tasks` และ `projects` ใน list

- [ ] **Step 6: Commit**

```bash
git add packages/shared/drizzle.config.ts packages/shared/drizzle/ packages/shared/package.json
git commit -m "feat(shared): add Drizzle migrations for tasks and projects"
```

---

## Task 7: Auth Route — Test First

**Files:**

- Create: `apps/api/src/__tests__/health.test.ts`
- Create: `apps/api/src/__tests__/auth.test.ts`
- Create: `apps/api/src/routes/auth.ts`
- Create: `apps/api/vitest.config.ts`

- [ ] **Step 1: Create `apps/api/vitest.config.ts`**

```typescript
import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    environment: 'node',
    setupFiles: [],
  },
});
```

- [ ] **Step 2: Write failing health test**

Create `apps/api/src/__tests__/health.test.ts`:

```typescript
import { describe, it, expect, beforeAll, afterAll } from 'vitest';
import { buildServer } from '../server.js';

describe('GET /health', () => {
  let server: Awaited<ReturnType<typeof buildServer>>;

  beforeAll(async () => {
    process.env.DATABASE_URL = 'postgresql://meshagent:meshagent@localhost:5432/meshagent';
    process.env.REDIS_URL = 'redis://localhost:6379';
    process.env.AUTH_EMAIL = 'admin@example.com';
    process.env.AUTH_PASSWORD = 'changeme123';
    process.env.JWT_SECRET = 'test-secret-that-is-at-least-32-characters-long';
    server = await buildServer();
  });

  afterAll(async () => {
    await server.close();
  });

  it('returns ok', async () => {
    const response = await server.inject({ method: 'GET', url: '/health' });
    expect(response.statusCode).toBe(200);
    expect(response.json()).toEqual({ status: 'ok' });
  });
});
```

- [ ] **Step 3: Run test to verify it fails first**

```bash
cd apps/api && pnpm test
```

Expected: PASS (health route ทำไว้แล้ว) — ถ้า fail ให้ตรวจสอบ Docker services รันอยู่ไหม

- [ ] **Step 4: Write failing auth tests**

Create `apps/api/src/__tests__/auth.test.ts`:

```typescript
import { describe, it, expect, beforeAll, afterAll } from 'vitest';
import { buildServer } from '../server.js';

describe('Auth routes', () => {
  let server: Awaited<ReturnType<typeof buildServer>>;

  beforeAll(async () => {
    process.env.DATABASE_URL = 'postgresql://meshagent:meshagent@localhost:5432/meshagent';
    process.env.REDIS_URL = 'redis://localhost:6379';
    process.env.AUTH_EMAIL = 'admin@example.com';
    process.env.AUTH_PASSWORD = 'changeme123';
    process.env.JWT_SECRET = 'test-secret-that-is-at-least-32-characters-long';
    server = await buildServer();
  });

  afterAll(async () => {
    await server.close();
  });

  describe('POST /auth/login', () => {
    it('returns token with correct credentials', async () => {
      const response = await server.inject({
        method: 'POST',
        url: '/auth/login',
        payload: { email: 'admin@example.com', password: 'changeme123' },
      });
      expect(response.statusCode).toBe(200);
      expect(response.json()).toHaveProperty('token');
    });

    it('returns 401 with wrong password', async () => {
      const response = await server.inject({
        method: 'POST',
        url: '/auth/login',
        payload: { email: 'admin@example.com', password: 'wrong' },
      });
      expect(response.statusCode).toBe(401);
    });

    it('returns 401 with wrong email', async () => {
      const response = await server.inject({
        method: 'POST',
        url: '/auth/login',
        payload: { email: 'other@example.com', password: 'changeme123' },
      });
      expect(response.statusCode).toBe(401);
    });
  });

  describe('GET /auth/me', () => {
    it('returns user info with valid token', async () => {
      const loginResponse = await server.inject({
        method: 'POST',
        url: '/auth/login',
        payload: { email: 'admin@example.com', password: 'changeme123' },
      });
      const { token } = loginResponse.json();

      const response = await server.inject({
        method: 'GET',
        url: '/auth/me',
        headers: { authorization: `Bearer ${token}` },
      });
      expect(response.statusCode).toBe(200);
      expect(response.json()).toEqual({ email: 'admin@example.com' });
    });

    it('returns 401 without token', async () => {
      const response = await server.inject({ method: 'GET', url: '/auth/me' });
      expect(response.statusCode).toBe(401);
    });
  });
});
```

- [ ] **Step 5: Run tests to verify they fail**

```bash
cd apps/api && pnpm test
```

Expected: FAIL — "Route not found" for /auth/login

- [ ] **Step 6: Create `apps/api/src/routes/auth.ts`**

```typescript
import { FastifyInstance } from 'fastify';
import { z } from 'zod';
import { env } from '../env.js';

const loginSchema = z.object({
  email: z.string().email(),
  password: z.string(),
});

export async function authRoutes(fastify: FastifyInstance) {
  fastify.post('/auth/login', async (request, reply) => {
    const body = loginSchema.parse(request.body);

    if (body.email !== env.AUTH_EMAIL || body.password !== env.AUTH_PASSWORD) {
      return reply.status(401).send({ error: 'Invalid credentials' });
    }

    const token = fastify.jwt.sign({ email: body.email }, { expiresIn: '30d' });
    return { token };
  });

  fastify.get('/auth/me', { preHandler: [fastify.authenticate] }, async (request) => {
    return { email: (request.user as { email: string }).email };
  });
}
```

- [ ] **Step 7: Add `authenticate` decorator to `apps/api/src/server.ts`**

เพิ่มหลัง `fastify.register(fjwt, ...)`:

```typescript
fastify.decorate('authenticate', async function (request: any, reply: any) {
  try {
    await request.jwtVerify();
  } catch {
    reply.status(401).send({ error: 'Unauthorized' });
  }
});
```

เพิ่ม import auth routes และ register:

```typescript
import { authRoutes } from './routes/auth.js';
// ...
await fastify.register(authRoutes);
```

เพิ่ม declaration ใน `declare module 'fastify'` (ใส่ที่ไหนก็ได้ใน server.ts):

```typescript
declare module 'fastify' {
  interface FastifyInstance {
    authenticate: (request: FastifyRequest, reply: FastifyReply) => Promise<void>;
  }
}
```

- [ ] **Step 8: Run tests to verify they pass**

```bash
cd apps/api && pnpm test
```

Expected: PASS — 5 tests pass

- [ ] **Step 9: Manual smoke test**

```bash
# Terminal 1: start API
cd apps/api && pnpm dev

# Terminal 2: test login
curl -s -X POST http://localhost:3001/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"admin@example.com","password":"changeme"}' | jq .
```

Expected: `{"token":"eyJ..."}`

- [ ] **Step 10: Commit**

```bash
git add apps/api/src/routes/auth.ts apps/api/src/server.ts apps/api/src/__tests__/ apps/api/vitest.config.ts
git commit -m "feat(api): add JWT auth login and /me endpoint with tests"
```

---

## Self-Review Checklist

- [x] Spec coverage: Foundation = monorepo ✓, shared types ✓, DB schema ✓, Docker Compose ✓, auth ✓
- [x] No placeholders: ทุก step มี code จริง
- [x] Type consistency: `AgentRole`, `KanbanStage`, `AgentStatus` ใช้ตรงกันใน types.ts และ schema.ts
- [x] Commands: ทุกคำสั่งมี expected output

---

## Next Plans

- **Plan 2:** Orchestration Service (agent subprocess runner + Redis streaming)
- **Plan 3:** Backend API (tasks, projects, WebSocket, GitHub webhooks)
- **Plan 4:** Frontend (Next.js PWA)
- **Plan 5:** Deployment (Docker Compose prod + DigitalOcean)
